# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage
.nyc_output

# Production
build/
dist/

# Misc
.DS_Store
.env
.env,example
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.idea/
.vscode/

# Server specific
server/uploads/
!server/uploads/.gitkeep
server/data/tests.json  # Depending on whether they want to commit sample data or ignore it? Let's ignore it if it's dynamic.
# Wait, tests.json is currently acting as a database. If I ignore it, the repo will deploy empty.
# However, usually data should not be committed. But for a small project fallback, maybe they WANT it committed. 
# The user said "push the work done till now". The JSON file contains their test data. I will NOT ignore tests.json for now, assuming it's initial seed data.
# But I WILL ignore admin-emails.json as it likely has private emails?
# User edited admin-email.json earlier.
# Let's interact with safe defaults. I will ignore .env as requested.

# Client specific
client/dist
client/.vite

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*
